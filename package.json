{
  "name": "minilogg-component-library",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=storybook",
    "storybook": "npm run storybook --workspace=storybook"
  }
}
